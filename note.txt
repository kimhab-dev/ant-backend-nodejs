+ ==========> other 
	+ view egin with backend mean backend with client like node with html 
	+ if we write return in for forEeach it's can stop loop but code below normal work.
=============> about node 
	+ cross platform work all os.
	+ node js code js del run in local 
	+ forEeach : min mean ey stop vea ban teh. if we want to set condition with loop
	+ why validate on fron and back : kapea oy ban jrem, custome message.
									+ node js vs javascript
	+ why js code egine jea ka convert code pii hight level tv low level (on brower completely have egin)
	+ v8 sgin for chrome, spider for
	+ nv in local machine don't have egin so we need install nodeJS for v8 view egin ()
	+ v8 egine 
	+ ey deal mean in node vea nv krom Golobal object khos pii brower vea nv krom window object
	+ derm3 use buildin module ban yg trov jam name vea and yg trov import nv khang ler ke 
	+ none blocking IO vea min wait ka tver ka out put 
	+ import must use ./ pel vea nv location tah muy 
	+ pel use frontend with backend hx ot use backend teh nus vea reload page 
	+ domain use for bit bang IP adrrest rbos website rbos yg
	+ module thom jeang pakage. buildin module ke create mk srab
	+ package if install global vea ot jol in package.jsom in project teh
	+ pel use post for put orr get for delete vea make oy slow. cuz vea tv delete data hx ban overide pii ler and ot standard

============> about dependency (package)
	+ npm : node package manager
	+ express : create API
	+ mogooes : connectDB
	+ dotenv : .env
	+ cors : request from flutter
	+ bcryptjs : hash password
	+ jsonwebtoken : create jwt token
	+ nodemon : help to auto restart server 
	+ nodeModule: ptuk code rbos package knong ng 
	+ npm init : create nodeModule, package.json in project
	+ loadash : help array, math, ......
	+ express-session : is a middleware in Node.js + Express used to create and manage user sessions on the server. it's can remember on server
	+ --> research 
		- express-async-handler
============>  error 
	+ node module not found is it's can not fine fial

+ ==========> between
	+ requirs vs import : it's same but requirs(in commons js), import(in ES module)

+ ==========> code and logic
	+ setInterval oy vea der rhot 
	+ file format : baseURL + dirname + filename (on db ke not store file. ke store only name)

+ ==========> file
	+ parameter in mkdirSync : ({recursive: true}) mean yg check mean mean folder orr nv 

+ ==========> server and client
	+ derm3 create server ban yg trov mean module server
	+ req trov nv muk jea nich
	+ req.query : get query string (pagination, filtering, search)
	+ req.header : get header (token, API key...)
	+ req.session : when using express-session 
	+ req.user : This is NOT built-in, but used everywhere
	+ localhost:  name domain rbos local computer is localhost(127.0.0.1)
	+ port: like tver rbos home yg 

+ ===========> Asychronous Operation 
	+ Asynchronous (Async) : A task runs in the background without blocking the rest of the program.
	+ make user can request same time.
	+ javascript doest not wait automatically

+ ===========> Middleware
	+ middleware is function ad mid between request and respones
	+ Middleware : a function that runs before your route executes.
	+ USE for check use request like login, ...... mk
	+ use with feature 4 thom thom :
		Logger : track, tam dan sokpheab rbos system, error issue, 
		Authentication : protect routes, 
		Middleware parse JSON : ke dak nv khang ler ke 
		Return 404 : now not make in backend for security

	+ oy tah see app.use is middleware all.
	+ middleware mean 2 : buildin and user define
	+ pel use user define must use next();

+ ===========> about API
	+ REST API : a standard way for client and server to communicate using HTTP.(get, post, put, delete, ...)
	+ restFull api : jea api del use http, have method get, post, put, delete......
	+ api have => deno, gradio........
	+ http://localhost:5000/api/products (baseURL + Resources). 
		- Resources : in server call Resources orr routes, frontend call endpoint, 
		- baseURL : in backend call HTTP protocol
	+ helmet kapea header rbos yg
	+ pel yg bos data pii kleng na mk kan backend rbos yg yg trov convert 
		vea tv jea JSON object rbos language muy ng. like express convert mk jea js object\

+ ===========> Node with data base 
	+ mySQL 2 new version : vea support with Asychronous function
	+ create pool : can handle multi request 
	+ derm connect tv kan DB ke trove ka connection string 
	+ khos port doy sa yg mean database jren (mySQL, xampp, Workbench) yg trov merl vea
	+ jek table : if data dulicate
+ ===========> Key word in DB 
	+ UNIQUE : cannot dulicate but can null 

+ ===========> MVC (Model view controller)
	-> model -> service -> controller
		=> model -> data & business rules handle schema 
		=> view -> user interface 
		=> controller -> handle user input, user req
	+ controller : handle only req 
	+ service : logic handle logic
	+ model : query to database
	+ route : handle route 
	+ repository :  

	-> 
		+ config : handle database, information somthing...
		+ controller : handle req user (call service, res json)
		+ model : handle database, query
		+ routes : endpoint, resources, 
		+ service : logic, (business logic)
		+ utils : hyper file, like store function for use 
	-> 
	+ We usually put try...catch in the controller because it’s the place that handles the HTTP response.
	+ if we want to use try catch in service can use for custome error and throw pass controller

+ ===========> Authentication
	+ kun som bat knong ka manage tv ler user profile by token 


+ => express session, export varible, ke tver error handller in middleware men teh 