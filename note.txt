+ ==========> other 
=============> about node 
	+ cross platform work all os.
	+ node js code js del run in local 
									+ node js vs javascript
	+ why js code egine jea ka convert code pii hight level tv low level (on brower completely have egin)
	+ v8 sgin for chrome, spider for
	+ nv in local machine don't have egin so we need install nodeJS for v8 view egin ()
	+ v8 egine 
	+ ey deal mean in node vea nv krom Golobal object khos pii brower vea nv krom window object
	+ derm3 use buildin module ban yg trov jam name vea and yg trov import nv khang ler ke 
	+ none blocking IO vea min wait ka tver ka out put 
	+ import must use ./ pel vea nv location tah muy 
	+ pel use frontend with backend hx ot use backend teh nus vea reload page 
	+ domain use for bit bang IP adrrest rbos website rbos yg
	+ module thom jeang pakage. buildin module ke create mk srab
	+ package if install global vea ot jol in package.jsom in project teh 

============> about dependency (package)
	+ npm : node package manager
	+ express : create API
	+ mogooes : connectDB
	+ dotenv : .env
	+ cors : request from flutter
	+ bcryptjs : hash password
	+ jsonwebtoken : create jwt token
	+ nodemon : help to auto restart server 
	+ nodeModule: ptuk code rbos package knong ng 
	+ npm init : create nodeModule, package.json in project
	+ loadash : help array, math, ......

============>  error 
	+ node module not found is it's can not fine fial

+ ==========> between
	+ requirs vs import : it's same but requirs(in commons js), requirs(in ES module)

+ ==========> code and logic
	+ setInterval oy vea der rhot 
	+ file format : baseURL + dirname + filename (on db ke not store file. ke store only name)

+ ==========> file
	+ parameter in mkdirSync ({recursive: true}) mean yg check mean mean folder orr nv 

+ ==========> server and client
	+ derm3 create server ban yg trov mean module server
	+ req trov nv muk jea nij
	+ localhost:  name domain rbos local computer is localhost(127.0.0.1)
	+ port: like tver rbos home yg 

+ ===========> Asychronous Operation 
	+ Asynchronous (Async) : A task runs in the background without blocking the rest of the program.
	+ make user can request same time.
	+ javascript doest not wait automatically

+ ===========> about API
	+ REST API : a standard way for client and server to communicate using HTTP.(get, post, put, delete, ...)
	+ Middleware : a function that runs before your route executes.
